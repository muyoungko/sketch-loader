<div class="col-md-6 grid-margin stretch-card">
  <div class="card">
    <div class="card-body">
      <h4 class="card-title">스캐치 파일 업로드 테스트</h4>
      <p class="card-description">
        스캐치 파일을 업로드 해주세요
      </p>
      <form class="forms-sample" action="/deploy" method="post" enctype="multipart/form-data">
        <div class="form-group">
          <label>File upload</label>
          <input id="file" type="file" name="file" class="file-upload-default">
          <div class="input-group col-xs-12">
            <input id="fileNameText" type="text" class="form-control file-upload-info" disabled placeholder="Upload Sketch">
            <span class="input-group-append">
              <button id="upload_button" class="file-upload-browse btn btn-info" type="button">Upload</button>
            </span>
          </div>
        </div>

        <script>
        $(function() {
            $("#upload_button").click(function(e){
               e.preventDefault();
               $("#file").trigger('click');
            });
            $("#file").change(function (){
             var fileName = $(this).val();
             $("#fileNameText").val(fileName);
            });
         });
        </script>

        <button type="submit" class="btn btn-success mr-2">Submit</button>
        <button class="btn btn-light">Cancel</button>
      </form>
    </div>
  </div>
</div>
